# In production, set this to your deployed backend URL (e.g. Render, Railway, Fly.io)
NEXT_PUBLIC_API_URL=https://your-backend.onrender.com/api

# In development, this is NOT needed — Next.js rewrites proxy /api/* -> localhost:8000
