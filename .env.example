# Production (Vercel multi-service): Set this in Vercel project env vars
# The backend runs at /_/backend via experimentalServices config in vercel.json
NEXT_PUBLIC_API_URL=/_/backend/api

# Development: NOT needed — Next.js rewrites proxy /api/* -> localhost:8000
